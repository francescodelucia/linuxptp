name: Release Build

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
    types: [closed]

jobs:
  release_web:
    name: -------
    runs-on: ubuntu-latest

    steps:
      - uses: owner/repo@ref
      - name: Configure Make
        run: make 
